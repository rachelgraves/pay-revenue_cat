revenue_cat:
  type: Pay::RevenueCat::Customer
  owner: revenue_cat (User)
  processor: revenue_cat
  processor_id: <%= ActiveRecord::FixtureSet.identify("revenue_cat") %>
  default: true

revenue_cat_account:
  type: Pay::RevenueCat::Customer
  owner: revenue_cat_account (Account)
  processor: revenue_cat
  processor_id: <%= ActiveRecord::FixtureSet.identify("revenue_cat_account") %>
  default: false
